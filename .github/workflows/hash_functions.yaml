name: Hash functions
on: push
jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ehco "${HASH}"
        env:
          HASH: ${{ hashFiles('.github/workflows/*.yaml') }}
